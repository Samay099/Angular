import { CompFaliureDirective } from './comp-faliure.directive';

describe('CompFaliureDirective', () => {
  it('should create an instance', () => {
    const directive = new CompFaliureDirective();
    expect(directive).toBeTruthy();
  });
});
